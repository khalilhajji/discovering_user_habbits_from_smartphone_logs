contains code that outputs a plot representing the number of records by day during the whole observation period for each user.

records_by_day_all_users.py is the funtion that outputs the plots for all the users (one plot by user)