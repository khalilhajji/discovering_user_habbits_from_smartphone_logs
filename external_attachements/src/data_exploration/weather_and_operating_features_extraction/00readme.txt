There is some features that we haven't seen (appOperatingTime and currentWeatherInfo).
The code in this folder just extract them to see the values that can take those features and their realizations.
The extracted sample will be in the log dir