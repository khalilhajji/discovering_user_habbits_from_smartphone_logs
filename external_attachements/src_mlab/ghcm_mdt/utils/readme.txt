This folder contains various useful functions which are not directly linked 
with model fitting:
- functions to display the dataset and parameters of the fitted model
- functions to generate synthetic data (to perform tests)
- function to convert data in the required format